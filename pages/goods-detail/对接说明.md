# 积分商品详情页（goods-detail）对接说明

按 Figma 设计稿（node `9-2949`，文件 `oWPw5JgcpsdIhgKuHGh5lX`）还原的积分兑换商品详情页。

- 详情页文件：`pages/goods-detail/goods-detail.vue` + `goods-detail.css`
- **旧通用电商详情页已备份**：`pages/goods-detail/goods-detail.old.vue`（1594 行）+ `goods-detail.old.css`（未在 `pages.json` 注册，不参与编译；如需回退，将其内容覆盖回 `goods-detail.vue`/`goods-detail.css` 即可）
- 文案：`locale/zh.json` / `locale/en.json` 的 `index-points.index-points.*`
- 设计稿宽 750，与 `750rpx` 1:1
- 入口参数：`?id=商品ID`（与原详情页一致）

## 已对接接口（复用通用商品详情接口，无需新增）

| 设计元素 | 接口 | 字段 |
|---|---|---|
| 主图轮播 | `get_request_url('detail','goods')` POST `{id}` | `data.goods.photo[].images`（无相册则用 `data.goods.images`） |
| 商品标题 | 同上 | `data.goods.title` |
| 副标题 | 同上 | `data.goods.simple_desc` |
| 剩余库存 | 同上 | `data.goods.inventory` |
| 图文详情 | 同上 | `data.goods.content_app[]`（移动端）/ `data.goods.content_web`（富文本） |
| 立即兑换 | 复用 `components/goods-buy` 组件 → 通用下单链路 | `data.buy_button` |

## TODO — 后端需要做的事

> 代码中所有 TODO 点均已用 `// TODO` 注释标注在 `goods-detail.vue` 对应位置。

### 1. 单品「兑换所需积分」字段 ⚠️ 必须确认/补充
- 前端位置：`computed.goods_points_view`
- 现状：临时按 `goods.exchange_integral || goods.give_integral || goods.price` 兜底取值。
- **后端需做**：在商品详情接口 `detail/goods` 返回的 `goods` 对象中，提供明确的**兑换所需积分**字段（建议如 `exchange_integral`），并告知字段名，前端改为唯一字段。

### 2. 积分兑换下单链路 ⚠️ 关键
- 前端位置：`exchange_event()` → `goods-buy` 组件 → 通用下单接口。
- 现状：复用通用「立即购买」链路（弹规格/数量 → 下单）。
- **后端需做**：
  - 确认 `data.buy_button` 配置支持「积分兑换」模式（按钮文案/行为）。
  - 下单接口需支持以**积分**作为支付方式：下单时校验用户积分余额、扣减积分、生成兑换订单（而非现金支付）。
  - 若积分商品有独立的兑换下单接口/参数（区别于普通商品），请提供，前端改为对应调用。

### 3. 库存「剩余 X 件」
- 字段：`goods.inventory`（已有）。
- **后端需做**：确认积分商品库存口径与该字段一致即可，无需改动。

### 4. 在线客服 🎨 待接入
- 前端位置：`service_event()`
- 现状：仅 Toast 占位。
- **后端需做**：确认后台客服功能已开启；前端后续接入 `components/online-service`。

### 5. 底部图标素材 🎨 待补素材
- 前端位置：`goods-detail.css` 的 `.bottom-icon-service` / `.bottom-icon-home`
- 现状：纯色占位。
- 需求：从 Figma 导出「客服 / 首页」图标放入 `static/` 后替换。
