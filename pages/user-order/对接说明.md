# 积分商城-我的订单（user-order）对接说明

按 Figma 设计稿（node `9-1896`）还原的订单列表页。

- 文件：`pages/user-order/user-order.vue` + `user-order.css`
- **旧订单页已备份**：`user-order.old.vue`(857行) + `user-order.old.css`（可回退）
- 改造方式：**完整保留原 script 逻辑**（支付/取消/确认收货/删除/物流/合并支付/搜索等全部方法与字段不变），仅替换 `<template>` 的导航与订单卡样式 + 重写 `.css`。
- 文案：合计行复用原 `user-order-detail.*` key；导航复用 `index-points.index-points.*`

## 已对接（复用 `index/order` 接口，无新增）

| 设计元素 | 字段 |
|---|---|
| 状态 Tab | `nav_status_list[]`（原逻辑，含全部/待付款/待发货/待收货…） |
| 订单状态 | `item.status_name` |
| 商品图/名/规格/单价/数量 | `item.items[].images/title/spec[].value/price/buy_number` |
| 合计 | `item.buy_number_count` / `item.total_price` |
| 操作按钮 | `item.operate_data`（is_pay/is_cancel/is_collect/is_comments/is_delete）+ 物流等，**全部保留** |

## TODO — 待后端确认

### 1. 订单日期字段 ⚠️ 待确认
- 前端位置：订单头 `item.add_time_time || item.add_time || item.warehouse_name`
- 现状：优先取格式化时间，兜底取仓库名。
- **后端需做**：确认订单列表项返回的下单时间字段名（设计左上角显示日期，如 `2017-11-16`）。

### 2. 积分单位显示
- 现状：单价/合计前缀用 `item.currency_data.currency_symbol`（现金订单为 ￥）。
- **后端需做**：积分兑换订单应让该字段为空或返回积分单位，使其显示为纯积分数（设计为"积分：1350"样式）。

### 3. 其余操作链路
- 支付（`pay_event`）、确认收货（`collect_event`）、取消、删除、物流均复用原有接口（`pay/order`、`collect/order`、`cancel/order`、`delete/order` 等），逻辑未改，确认后端这些接口在新后端 `xunxiang.ysfaedu.com` 正常即可。
