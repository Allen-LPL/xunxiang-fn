# 积分商城-兑换成功页（exchange-success）对接说明

按 Figma 设计稿（node `9-2459`）新建的独立页面。

- 文件：`pages/exchange-success/exchange-success.vue` + `.css`（**新建**）
- 路由：`pages.json` 已注册（主包，`navigationStyle: custom`）
- 文案：`index-points.index-points.*`
- 内容：金色对勾 + "兑换成功" + "下单后请求授权，接收物流状态通知！" + 是/否按钮

## 交互

- **是** → 微信订阅消息授权（`uni.requestSubscribeMessage`）后跳「我的订单」
- **否** → 直接跳「我的订单」（`redirectTo /pages/user-order/user-order`）

## TODO

### 1. 订阅消息模板ID ⚠️
- 前端位置：`auth_yes_event()` 的 `tmplIds: []`
- 现状：模板ID 数组为空（点"是"不会真正订阅）。
- **后端需做**：提供「物流状态通知」的微信订阅消息**模板ID**（建议随某接口下发），前端填入 `tmplIds`。

### 2. 接入跳转 ⚠️ 待接入
- 现状：此页面已就绪，但**还没有任何地方跳转到它**。
- **需做**：在**积分兑换下单成功**后跳转到本页。下单链路走 `components/goods-buy` → 通用下单接口，成功回调里加：
  `uni.redirectTo({ url: '/pages/exchange-success/exchange-success?order_id=xxx' })`
  （具体在哪个成功回调接入，取决于积分兑换下单走的支付/下单流程，确认后接入。）
