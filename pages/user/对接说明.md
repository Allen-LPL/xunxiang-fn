# 积分商城个人中心（user）对接说明

按 Figma 设计稿（node `9-3349`，文件 `2OnrcoHiY9oOyMzQFgzvVa`）还原的积分商城会员中心。

- 页面文件：`pages/user/user.vue` + `user.css`（tabbar 第 4 页）
- **旧个人中心页已备份**：`pages/user/user.old.vue` + `user.old.css`（未注册，不参与编译；回退时覆盖回 `user.vue`/`user.css` 即可）
- 文案：`locale/zh.json` / `locale/en.json` 的 `index-points.index-points.*`
- 底部 Tab 与卡片图标均为**内联矢量 SVG**（base64 注入在 `user.css` 末尾），无需图片素材

## 已对接接口（复用用户中心接口，无需新增）

| 设计元素 | 接口 | 字段 |
|---|---|---|
| 头像 / 昵称 | `get_request_url('center','user')` POST | `data.avatar` / `data.user_name_view`（兜底本地缓存 `user`） |
| 积分余额 | 同上 | `data.integral` |
| 我的订单各状态角标 | 同上 | `data.user_order_status[]`（`status`：1待付款/2待发货/3待收货/101售后，`count`） |
| 积分明细 | 跳转 | `/pages/user-integral/user-integral` |
| 收获地址管理 | 跳转 | `/pages/user-address/user-address` |
| 我的订单/查看更多 | 跳转 | `/pages/user-order/user-order` |

## TODO — 后端需要做的事

> 代码中均已用 `// TODO` 注释标注在 `user.vue` 对应位置。

### 1. 「已兑换礼劵」数量字段 ⚠️ 待后端补充
- 前端位置：`get_data()` 的 `exchange_coupon_count`
- 现状：临时取 `data.exchange_coupon_count`，无则显示 0。
- **后端需做**：在 `center/user` 接口返回用户**已兑换礼劵/优惠券数量**字段（建议 `exchange_coupon_count`）。

### 2. 「签到」入口 ❌ 待确认页面
- 前端位置：`signin_event()`
- 现状：跳 `/pages/plugins/signin/index/index`（猜测路径）。
- **后端/前端需做**：确认签到(signin)插件已启用及其正确页面路径；若未启用需后台开启。

### 3. 「兑换记录」页面 ❌ 待确认
- 前端位置：`exchange_record_event()`
- 现状：临时跳 `/pages/user-order/user-order`。
- **后端需做**：提供积分兑换订单的列表来源（订单列表按积分支付筛选，或独立接口），并确认目标页。

### 4. 「客服入口」🎨 待接入
- 前端位置：`service_event()`
- 现状：仅 Toast 占位。
- **后端需做**：确认后台在线客服功能开启；前端后续接入 `components/online-service` 或客服电话 `common_app_customer_service_tel`。

### 5. 「常见问题」页面 ❌ 待确认
- 前端位置：`faq_event()`
- 现状：仅 Toast 占位。
- **后端需做**：提供常见问题/帮助页地址（通常为某篇文章详情 `/pages/article-detail/article-detail?id=x`）。
