# 支付结果页（paytips）对接说明

页面：`pages/paytips/paytips.vue`（+ `paytips.css`）
设计稿：Figma `2OnrcoHiY9oOyMzQFgzvVa` 节点 `9-2459`（兑换成功/结果页风格）
旧页备份：`paytips.old.vue` / `paytips.old.css`（未注册，不编译，可回退）

## 已完成（换皮，逻辑零改动）

- 按 Figma 金色风格重做：米色背景 + 金色自定义导航（返回 + 居中标题「支付结果」）+ 圆形结果图标 + 结果文案 + 两个按钮。
- 复用 `pages/exchange-success` 同一套金色规范（导航/圆形图标/按钮样式一致）。
- **完整保留原有支付结果逻辑**：
  - `onLoad` 里 base64 解参、`code` 状态机（9000 成功 / 8000 处理中 / 4000 失败 / 6001 取消 / 6002 网络错误 / 6004 未知 / 99 / default）→ `params.msg` 文案不变（沿用 `paytips.paytips.*` 多语言）。
  - 结果图标：`code == '9000'` 显示金色对勾圆，其余显示红色叉号圆（纯 CSS 绘制，未用图片素材；原 `default_round_success_icon` / `default_round_error_icon` 字段保留在 data 中未删，备用）。
  - 主按钮（金色）= 原跳转按钮：`@tap="url_event"`、`data-redirect="1"`、`:data-value="default_to_url"`，文案 `params.title || $t('paytips.paytips.jifuu8')`。
  - 次按钮（白描边）= 返回：`@tap="back_event"`（`page_back_prev_event()`）。
- `pages.json` 已为该页加 `navigationStyle: custom`（`#ifdef MP-WEIXIN || MP-BAIDU || MP-QQ || MP-KUAISHOU || APP`，与 exchange-success 一致；H5/支付宝小程序保持默认导航）。

## 备注 / 待确认

- 导航标题固定为「支付结果」（硬编码中文，未走 i18n）。若需多语言，可在 `paytips` 命名空间补一个 key 再替换。
- 设计稿里的副标题「下单后请求授权，接收物流状态通知！」属于 9-2459 的“兑换成功+订阅授权”场景（已在 `pages/exchange-success` 实现），支付结果页语义不同，故未照搬；paytips 仅保留 `params.msg` 作为主文案，未加副标题。
- 配色为积分商城金色专用方案，写死在 `paytips.css`，与全局 8 套 theme 无关。
- 设计稿宽 750，与 750rpx 1:1，标注 px 直接当 rpx。
