# 积分商城-地址管理（user-address）对接说明

按 Figma 设计稿（node `9-1723`）还原。

- 文件：`pages/user-address/user-address.vue` + `user-address.css`
- **旧页已备份**：`user-address.old.vue` + `user-address.old.css`
- 改造方式：保留原 script（列表/选择/微信导入逻辑），替换 template+css，**新增「删除」「设为默认」两个操作**。
- 导航：`pages.json` 已给 user-address 加 `navigationStyle: custom`（金色自定义导航）。
- 文案：`index-points.index-points.*`；图标（编辑/删除）内联 SVG。

## 已对接（复用现有接口）

| 操作 | 接口 |
|---|---|
| 地址列表 | `index/useraddress` |
| 删除地址 | `delete/useraddress` POST `{id}`（复用 user-address-save 既有接口） |
| 从微信导入 | `outsystemadd/useraddress`（原逻辑 `uni.chooseAddress`） |
| 编辑/新增 | 跳转 `user-address-save` |

## TODO — 待后端确认

### 「设为默认」接口 ⚠️
- 前端位置：`setdefault_event()`
- 现状：临时调 `save/useraddress` 传 `{id, is_default:1, name, tel, province, city, county, address}`。
- **后端需做**：确认是否有**独立设默认接口**（如 `setdefault/useraddress` 只传 id）；若 `save/useraddress` 要求完整字段且字段名不同（如 province 是否传 id），请告知，前端调整。
